{
  "name": "pulse",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A browser-based turn-based physics chain reaction game.",
  "scripts": {
    "dev": "py -m http.server 8080",
    "test": "node --test tests/*.test.mjs"
  }
}