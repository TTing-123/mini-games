# Mini Games 开发规则

## 仓库定位

浏览器即时游玩的小游戏合集。每个游戏是独立的纯静态子目录，根 `index.html` 是合集首页。

## 新游戏必须过两道关

### 第一关：开工前先做市场调研（硬性，不许跳）

先跑：

```powershell
node tools/market-check.mjs "机制关键词"
```

再看头部作品本身（Steam 商店页、itch 页面），回答三个问题：

1. 这个机制已有的头部作品是什么？做到什么程度？
2. 我打算做的那一版，和它最明显的差别是什么？
3. 如果差别只是「画面不同」「我做得更简单」，那就别做。

判定参考：

- Steam 命中几十个同类、头部评价过万 → 除非有硬差异化，否则放弃
- 只有零散 jam 作品、没有口碑标杆 → 优先做
- 命中数少只说明没人用这个词，**不代表玩法空白**，必须回到「头部作品长什么样」再判断

调研结论写进 `_bmad-output/`（或游戏目录里的 `NOTES.md`），不要只留在聊天里。

### 第二关：10 分钟可玩原型

原型必须能实际运行、能操作、能玩完一局，并至少完成一次真人 playtest，记录「继续 / 调整 / 放弃」。

## 技术约束

- 纯静态 HTML / CSS / JS，无构建工具，`py -m http.server` 就能跑
- 逻辑与渲染分离：`<game>/src/<name>-core.js` 放纯逻辑（可单测、不碰 DOM），`<game>/src/game.js` 只做 Canvas 渲染与界面交互
- 每个游戏自带 `tests/`，根 `npm test` 必须能一次跑完所有游戏
- localStorage 键带游戏前缀（如 `pulse-endless-depth`），避免游戏之间撞名
- 低美术低音乐：程序化几何视觉 + 音效反馈，不引入图片和音乐素材

## 玩法约束

- 规则一句话能说清，目标一眼看得懂，少文本多视觉
- 要「简单规则 + 自发复杂」：玩家动作少，系统反馈多
- 教学内嵌在前几关，不做独立全屏教程；机关说明直接贴在场内
- 反例：靠反应速度、靠堆数值、放置挂机

## 提交规范

`type(scope): 中文摘要`，只写重点，细节交给 diff。