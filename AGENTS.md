# 游戏项目开发规则

## 项目定位

`pulse` 是一个低美术、低音乐、无建模的 2D 回合制物理连锁游戏。

核心幻想：玩家只负责瞄准和发射，弹球与不同颜色机关组合后产生连锁反应。玩家通过试验而不是文字说明理解规则。

## 默认开发流程

1. 新玩法默认先走 GDS Quick Flow。
2. 使用 `/bmad-help` 做流程路由，使用 `gds-quick-dev` 实现小步迭代。
3. 在核心原型通过验证前，不开始完整 GDD、架构和正式 Sprint 规划。
4. 原型必须实际运行、可操作，并完成至少一次人工 Playtest。
5. 记录 Playtest 反馈和“继续 / 调整 / 放弃”的判断。
6. 只有确认核心玩法值得继续后，才进入 Full Production。

## 项目约束

- 引擎固定为 Godot 4.7.2。
- 只做 2D，不使用 3D 模型。
- 不使用外部美术资源完成核心原型；正式资源需先确认。
- 基础玩法不依赖文本教程。
- 不设实时倒计时，不考核反应速度。
- 玩家主动作保持为瞄准和发射。
- 每种颜色机关必须通过一次自然碰撞即可理解其主要效果。
- 黄块分裂必须限制为一次，禁止无限指数增长。\n- 绿色充能机关必须限制为单次触发，禁止无限补充发射次数。\n- 手工四关不得被程序化生成替换，无尽模式必须使用独立容器。\n- 无尽模式实体数量必须设有硬上限。
- 碰撞结果必须直接可见，不能依赖隐藏几何计算。
- 音乐不是必需品，优先使用碰撞、分裂、爆炸和引力反馈音效。

## 场景与脚本架构

- 静态棋盘、关卡、墙壁、机关、目标、出生点、准星和 HUD 必须定义在 `.tscn` 场景中。
- 脚本只实现状态、行为、输入协调、查询和信号。
- 弹球等动态实体必须来自 `PackedScene`。
- 优先使用 `@onready`、`@export` 和信号连接。
- 禁止把 `main.tscn` 留成空场景，再在脚本中动态创建整棵节点树。

## AI 工具协作

- Claude Code 和 Codex 共享 `_bmad/`、`_bmad-output/` 和游戏源码。
- 同一工作区同一时间只允许一个工具主导文件编辑。
- 并行开发使用不同 Git 分支或 worktree。
- 关键决策写入仓库工件，不依赖聊天记录。